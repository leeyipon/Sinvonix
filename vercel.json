{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && (prisma migrate deploy || echo '⚠ prisma migrate deploy failed — continuing build') && next build"
}
